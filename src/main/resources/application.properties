## APPLICATION CONFIGURATION
spring.profiles.active=${SPRING_PROFILES_ACTIVE:test}
server.port=${PORT:8080}
app.version=${APP_VERSION:2}
app.server.url=${APP_SERVER_URL:http://localhost:8080}
app.secret=${APP_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
app.environment=${APP_ENVIRONMENT:staging}
app.email.sender=${APP_EMAIL_SENDER:gervasioarthur@gmail.com}
app.email.password=${APP_EMAIL_PASSWORD:cgty tvbn qath mcoj}
spring.application.name=${SPRING_APPLICATION_NAME:walletwise-api}
## DATABASE CONFIGURATION
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/walletwise-database}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:walletwise}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:walletwise}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
## CONFIGURING CACHE
spring.cache.type=redis
spring.data.redis.url=${SPRING_DATA_REDIS_URL:redis://localhost:6379/0}
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.database=${SPRING_DATA_REDIS_DATABASE:1}
spring.data.redis.username=${SPRING_DATA_REDIS_USERNAME:default}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:0199}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.timeout=60000
## SENTRY CONFIGURATION
sentry.environment=${APP_ENVIRONMENT:staging}
sentry.server-name=${APP_SERVER_URL:http://localhost}